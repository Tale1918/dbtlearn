version: 2

sources:
  - name: airbnb
    schema: RAW
    tables:
      - name: listings
        identifier: raw_listings   # not raw_listings1, unless that’s really the table
      - name: hosts
        identifier: raw_hosts      # <-- fix here
      - name: reviews
        identifier: raw_reviews
        loaded_at_field: date
        freshness:
          warn_after: 
            count: 1
            period: hour
          error_after: 
            count: 24
            period: hour
